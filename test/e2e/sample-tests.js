const { expect } = require('chai');

describe('Simple Usage2', () => {
  it('some tests2', () => {
    expect(true).to.be.true;
  }); it('some tests2', () => {
    expect(true).to.be.true;
  }); it('some tests2', () => {
    expect(true).to.be.true;
  }); it('some tests2', () => {
    expect(true).to.be.true;
  });
});
